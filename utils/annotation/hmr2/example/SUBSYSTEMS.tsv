# TO BE USED TO INSERT MODEL ANNOTATIONS IN METABOLIC ATLAS DATABASE
# for SUBSYSTEM ONLY
# annotations will be visible in the GEM browser pages and some can be used by the search engine
#
# some rules:
# this is a TAB separated file, thus TABs are not allowed in any field values
# each line added must have exactly 4 columns to be parsed properly
# do not modify the @ line
# column 'ID' is mandatory, other columns are optional
# external_id / external_link must be filled as pair to be valid
# follow the max size limits
#
# columns description:
# column name   | max size | description
# ----------------------------------------
# ID            | 100      | corresponds to the name of the subsystem in the model
# external_id   | 50       | another identifier from an external database or another model
# external_link | 255      | full URL to associate with the ‘external_id’ column
# description   | 3000     | text description about the subsystem
#
# In this line specify the name of external databases used for each external_id column:	[external_id]		
# e.g. replace [external_id] with "KEGG" if the 'external_id' column contains KEGG IDs and the 'external_link' column KEGG links.
#
@ID	external_id	external_link	description
[start here]
